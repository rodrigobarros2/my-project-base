/* O createBrowserHistory do history facilita para uma página suportar coisas como marcadores e botão de volta, ele armanena os valores de navegação. */

import { createBrowserHistory } from 'history';

const history = createBrowserHistory();

export default history;
